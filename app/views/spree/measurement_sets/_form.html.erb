<%= form_for(@measurement_set) do |f| %>
  <% if @measurement_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@measurement_set.errors.count, "error") %> prohibited this measuremnt from being saved:</h2>

      <ul>
      <% @measurement_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
